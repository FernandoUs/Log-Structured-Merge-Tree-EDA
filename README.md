# Log-Structured-Merge-Tree-EDA

Compilación mínima (desde la raíz del proyecto):

```zsh
mkdir build
cd build
cmake ..
make
```

Después de `make` el ejecutable `lsm_spatial_db` quedará en la carpeta `build/`.